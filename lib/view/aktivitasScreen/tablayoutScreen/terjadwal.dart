import 'package:flutter/material.dart';

class TerjadwalTab extends StatelessWidget {
  const TerjadwalTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Halaman Terjadwal',
        style: TextStyle(fontSize: 16),
      ),
    );
  }
}
