import 'package:flutter/material.dart';

class ProsesTab extends StatelessWidget {
  const ProsesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Halaman Proses',
        style: TextStyle(fontSize: 16),
      ),
    );
  }
}
